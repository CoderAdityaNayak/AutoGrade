# AutoGrade
AutoGrade is a Desktop app that automatically checks answer sheets using AI. It captures photos, extracts marks and types them automatically— reducing manual marks entry time from minutes to seconds.
